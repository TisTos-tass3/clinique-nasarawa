{% extends 'base.html.twig' %}

{% block title %}Gestion des hospitalisations{% endblock %}

{% block body %}
<div class="container mt-4">
    <h2>Liste des hospitalisations</h2>

    <a href="{{ path('hospitalisation_new') }}" class="btn btn-primary mb-3">
    Ajouter une hospitalisation
    </a>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
            <th>ID</th>
            <th>Patient</th>
            <th>Motif</th>           
            <th>Médecin Réf.</th>     
            <th>Date entrée</th>
            <th>Date sortie</th>
            <th>Statut</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
            {# BACKEND : Remplacer les lignes d'exemple par une boucle for #}
            {# {% for hospitalisation in hospitalisations %} #}
            <tr>
            <td>1</td>
            <td>Maman Sani</td>
            <td>
                {# hospitalisation.motifAdmission #}
                Observation post-opératoire
            </td>
            <td>
                {# hospitalisation.medecinReferent.nom #}
                Dr. Traoré
            </td>
            <td>10/02/2026</td>
            <td>-</td>
            <td><span class="badge bg-info">En cours</span></td>
            <td>
                <a href="{{ path('hospitalisation_show') }}" class="btn btn-info btn-sm">Voir</a>
                <a href="#" class="btn btn-warning btn-sm">Modifier</a>
                <a href="#" class="btn btn-danger btn-sm">Supprimer</a>
            </td>
        </tr>
            {# {% endfor %} #}
        </tbody>
    </table>
</div>
{% endblock %}