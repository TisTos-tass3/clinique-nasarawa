<form method="post" action="{{ path('app_prescription_ligne_delete', {'id': prescription_ligne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prescription_ligne.id) }}">
    <button class="btn">Delete</button>
</form>
