<form method="post" action="{{ path('app_traitement_hospitalisation_delete', {'id': traitement_hospitalisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traitement_hospitalisation.id) }}">
    <button class="btn">Delete</button>
</form>
