<form method="post" action="{{ path('app_service_medical_delete', {'id': service_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_medical.id) }}">
    <button class="btn">Delete</button>
</form>
