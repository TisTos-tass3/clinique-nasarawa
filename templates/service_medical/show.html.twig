{% extends 'base.html.twig' %}

{% block title %}ServiceMedical{% endblock %}

{% block body %}
    <h1>ServiceMedical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_medical.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ service_medical.libelle }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ service_medical.createdAt ? service_medical.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ModifiedAt</th>
                <td>{{ service_medical.modifiedAt ? service_medical.modifiedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_medical_index') }}">back to list</a>

    <a href="{{ path('app_service_medical_edit', {'id': service_medical.id}) }}">edit</a>

    {{ include('service_medical/_delete_form.html.twig') }}
{% endblock %}
